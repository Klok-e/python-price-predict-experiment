[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "netgrowth"
version = "0.1.0"
description = "Causal direct Net Log Growth portfolio-policy experiment"
requires-python = ">=3.13,<3.14"
dependencies = [
  "fastapi>=0.115",
  "numpy>=2.2",
  "pandas>=2.2",
  "torch==2.13.0+rocm7.2",
  "triton-rocm==3.7.1; sys_platform == 'linux'",
  "uvicorn>=0.34",
]

[project.scripts]
netgrowth = "netgrowth.cli:main"

[dependency-groups]
dev = [
  "httpx>=0.28",
  "mypy>=1.15",
  "pandas-stubs>=2.2",
  "pytest>=8.3",
  "ruff>=0.11",
]

[[tool.uv.index]]
name = "pytorch-rocm"
url = "https://download.pytorch.org/whl/rocm7.2"
explicit = true

[tool.uv.sources]
torch = { index = "pytorch-rocm" }
triton-rocm = { index = "pytorch-rocm" }

[tool.hatch.build.targets.wheel]
packages = ["netgrowth"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 120
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.13"
strict = true
packages = ["netgrowth"]
